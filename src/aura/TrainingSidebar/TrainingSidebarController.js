({
    toggleBadges : function(component, event, helper) {
        helper.toggleBadges(component, event);
    },
    runAs : function(component, event, helper) {
        helper.runAs(component, event);
    },
    freeSearch : function(component, event, helper) {
        helper.freeSearch(component, event);
    },
    topicSearch : function(component, event, helper) {
        helper.topicSearch(component, event);
    },
    clearSearch : function(component, event, helper) {
        helper.clearSearch(component, event);
    }
})